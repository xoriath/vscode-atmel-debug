'use strict';

import { IContextListener } from './../IContext';
import { ILineNumberContext } from './ILineNumberContext';

export interface ILineNumberListener extends IContextListener<ILineNumberContext> {

}