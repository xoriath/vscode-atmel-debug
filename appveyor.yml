
environment:
  VSCODE_BUILD_VERBOSE: true

install:
  - ps: Install-Product node 7.8.0 x64
  - npm install -g npm vsce > log.txt
  - npm install >> log.txt

cache:
  - node_modules

build_script:
  - npm run package

test_script:
  - node --version
  - npm --version
  - npm install -g appveyor-mocha >> log.txt
  - npm test

artifacts:
  - path: '**\*.vsix'

  - path: 'log.txt'
    name: build logs
